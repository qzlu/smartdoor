/**   
 * api接口统一管理
 * Login 登录
 * 详情请看接口文档
 */
import {post } from './http';
/* const Login = p => post('/api/checklogin', {
    controller:'',
    requestMapping:'checklogin',
    operaterUserId:'maria',
    paramJSON:p
}); //登录接口
const baseUrl = '/api/'
const Post = (controller,mapping,p,load = false) => post(baseUrl,{
    controller:controller,
    requestMapping:mapping,
    operaterUserId:'maria',
    paramJSON:{
        ...p
    }
},load)
const Project = (mapping,p,load = false) => Post('projectInfoController',mapping,p,load) */
/* export{
    Login,
    Project
} */